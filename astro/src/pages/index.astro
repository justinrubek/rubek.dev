---
import DefaultPageLayout from '$/layouts/default.astro'
import PostPreviewList from '$/components/PostPreviewList.astro'


const title = 'Home'
const description = 'rubek.dev'

const posts = await Astro.glob('./posts/*.md*')
---
<DefaultPageLayout content={{ title, description }} showPageHeader={false}>
    <PostPreviewList posts={posts.slice(0, 2)} />
    <div class="page__actions">
        <a class="action__navigate-posts href="/posts" title="All Posts">posts &rarr;</a>
    </div>
</DefaultPageLayout>

<style>
    .page__actions {
        @apply flex justify-center md:justify-end py-6
    }
    .action__navigate-posts {
        @apply text-base uppercase text-gray-500 dark:text-gray-400 hover:underline
    }
</style>
